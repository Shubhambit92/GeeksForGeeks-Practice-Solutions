class Solution 
{
    static String delAlternate(String S) 
    {
        int l=S.length();
        String out="";
        for(int i=0;i<l;i+=2)
        {
            char ch=S.charAt(i);
            out=out+Character.toString(ch);
        }
        return out;
    }
}
