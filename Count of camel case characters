class Sol
{
    int countCamelCase (String s)
    {
        int c=0,l=s.length();
        for(int i=0;i<l;i++)
        if(Character.isUpperCase(s.charAt(i))){
        c++;
        }
        return c;
    }
}
